body {
    font-family: 'Vazir', sans-serif; /* فونت فارسی */
    background-image: url('background.jpg'); /* تصویر زمینه */
    background-size: cover;
    color: #333;
    margin: 0;
    padding: 0;
}

header {
    text-align: center;
    padding: 20px;
    background-color: rgba(255, 255, 255, 0.8);
}

h1 {
    font-size: 2.5em;
}

main {
    padding: 20px;
}

footer {
    text-align: center;
    padding: 10px;
    background-color: rgba(255, 255, 255, 0.8);
}
